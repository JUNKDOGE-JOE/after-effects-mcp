{
  "mcpServers": {
    "ae": {
      "command": "python",
      "args": [
        "-m", "uv", "run",
        "--directory", "<PATH_TO_THIS_REPO_OR_PIP_INSTALL_LOCATION>",
        "python", "-m", "ae_mcp"
      ],
      "env": {
        "AE_MCP_BACKEND": "<aebm | other-installed-backend>",
        "AE_BRIDGE_ROOT": "<only if AE_MCP_BACKEND=aebm — path to AEBMethod plugin checkout>"
      }
    }
  }
}
